const menuBtn=document.querySelector('.menu-btn');const mobileNav=document.querySelector('.mobile-nav');if(menuBtn&&mobileNav){menuBtn.addEventListener('click',()=>mobileNav.classList.toggle('open'));mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileNav.classList.remove('open')))}const cookie=document.querySelector('.cookie');if(cookie&&localStorage.getItem('hey-cookie-choice'))cookie.style.display='none';document.querySelectorAll('[data-cookie]').forEach(btn=>btn.addEventListener('click',()=>{localStorage.setItem('hey-cookie-choice',btn.dataset.cookie);if(cookie)cookie.style.display='none'}));const track=document.querySelector('.projects-track');const dots=[...document.querySelectorAll('.dot')];let slideIndex=0;function updateDots(){dots.forEach((d,i)=>d.classList.toggle('active',i===slideIndex))}function move(dir=1){if(!track)return;const card=track.querySelector('.project-card');if(!card)return;const step=card.getBoundingClientRect().width+16;const visible=innerWidth<=860?1:3;const max=Math.max(0,track.children.length-visible);slideIndex=Math.max(0,Math.min(max,slideIndex+dir));track.scrollTo({left:step*slideIndex,behavior:'smooth'});updateDots()}document.querySelector('.slider-btn.next')?.addEventListener('click',()=>move(1));document.querySelector('.slider-btn.prev')?.addEventListener('click',()=>move(-1));if(track){setInterval(()=>{const visible=innerWidth<=860?1:3;const max=Math.max(0,track.children.length-visible);if(max<1)return;slideIndex=slideIndex>=max?-1:slideIndex;move(1)},4500)}const form=document.querySelector('#contact-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();const notice=document.querySelector('.notice');if(notice){notice.style.display='block';notice.textContent='Tak! Formularen virker som demo. Vi kobler den rigtige e-mail på, når kontaktoplysningerne er klar.'}form.reset()})}